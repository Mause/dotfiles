{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "configMigration": true,
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/(^|/)lazy-lock\\.json$/"
      ],
      "matchStrings": [
        "\"(?<depName>[^\"]+)\": { \"branch\": \"(?<branch>[^\"]+)\", \"commit\": \"(?<currentValue>[^\"]+)\" }"
      ],
      "depNameTemplate": "https://github.com/{{# if (equals depName 'folke') }}trouble.nvim{{/if}}{{# if (equals depName 'nvim-treesitter') }}nvim-treesitter{{/if}}{{# if (equals depName 'williamboman') }}mason.nvim{{/if}}{{# if (equals depName 'williamboman') }}mason-lspconfig.nvim{{/if}}{{# if (equals depName 'neovim') }}nvim-lspconfig{{/if}}{{# if (equals depName 'mrcjkb') }}rustaceanvim{{/if}}{{# if (equals depName 'github') }}copilot.vim{{/if}}{{# if (equals depName 'folke') }}lazy.nvim{{/if}}/{{depName}}",
      "versioningTemplate": "git",
      "datasourceTemplate": "git-refs"
    }
  ],
  "logLevelRemap": [
    {
      "matchMessage": "/^Dependency extraction complete/",
      "newLogLevel": "info"
    }
  ],
  "git-submodules": {
    "enabled": true
  }
}